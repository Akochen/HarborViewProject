@using WebApplication3.HelperClasses
@model List<CatalogCourse>
@{
    Layout = null;
    List<SelectListItem> courseList = new List<SelectListItem>();
    foreach (var v in Model)
    {
        courseList.Add(new SelectListItem { Value = v.id, Text = v.fullName });
    }
}
@Html.DropDownList("prereqID", courseList, null, new { @id = "prereqID"})
<input type="submit" value="Add" onclick="return checkAddedPrereq()" />